# Landslide_Early_Warning_Algorithm
These two codes are independent of each other. Both include an early warning algorithm designed around monitoring landslides, specifically debris flows, with seismo-acoustics sensors. The V2 version is designed to simulate a version of the algorithm onboarded onto a sensor, while the V3 simulates a sensor array sending data to a central system.
